# TensorFlow.js and TensorFlow Lite intro

## TensorFlow.js

First you need to install node.js first.
visit [node.js](https://nodejs.org/en/)

Then install `yarn` package manager.

```bash
npm install -g yarn
```
